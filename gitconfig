[alias]
  co = checkout
  dc = diff --cached
  lg = log --graph --pretty=format:' %C(red)%h%C(reset) - %s\n %C(blue)<%an> %C(green)%ar%C(yellow)%d%C(reset)\n'
  st = status
  pr = remote prune
[color]
  ui = auto
[user]
  name = Scott Nielsen
  email = scottnielsen5@gmail.com
[core]
  excludesfile = ~/.gitignore
[github]
  user = scizo
[push]
  default = upstream
[filter "trailingwhitespace"]
  clean = sed 's/[ \t]*$//'
  smudge = cat
[hub]
  host = code.savvysherpa.com
[credential]
  helper = osxkeychain
[url "git@github.com:"]
  insteadOf = https://github.com/
